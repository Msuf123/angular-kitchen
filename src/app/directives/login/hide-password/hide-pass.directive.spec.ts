import { HidePassDirective } from './hide-pass.directive';

describe('HidePassDirective', () => {
  it('should create an instance', () => {
    const directive = new HidePassDirective();
    expect(directive).toBeTruthy();
  });
});
