import { ClickNavigationDirective } from './click-navigation.directive';

describe('ClickNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
