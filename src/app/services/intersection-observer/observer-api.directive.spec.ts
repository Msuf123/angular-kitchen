import { ObserverApiDirective } from './observer-api.directive';

describe('ObserverApiDirective', () => {
  it('should create an instance', () => {
    const directive = new ObserverApiDirective();
    expect(directive).toBeTruthy();
  });
});
